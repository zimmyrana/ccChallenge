//task6 

let score = 80;
const age = 30;

console.log(score)
console.log(age)

//we can reassign value if we declare variable using let
score = score + 10;
// uncomment line no 13 to see the error
// we cannot reassign value if we decare const variable it will throw this error  TypeError: Assignment to constant variable.
// age = age + 1;

console.log(age)